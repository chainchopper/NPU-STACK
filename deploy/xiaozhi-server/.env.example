# XiaoZhi server secrets/config — copy to `.env` and fill in.
# NEVER commit `.env`.

# MySQL root password (change from the default!)
MYSQL_PASSWORD=changeme

# Docker image tag for the three xiaozhi images (pin to a released version).
XIAOZHI_TAG=0.6.4
